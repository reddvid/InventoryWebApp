@model ASPNETWebApp48.Models.Purchase
@if (User.IsInRole("purchmgr") || User.IsInRole("salesmgr"))
{
    <h3 class="text-center">@ViewBag.Message</h3>
}
@if (User.IsInRole("invmgr") || User.IsInRole("admin"))
{
    ViewBag.Title = "Check Product - " + @Model.Supplier.Name;

    <div class="container-fluid">
        <div class="card" style="margin-bottom: 20px">
            <div class="card-body">
                <h2>@ViewBag.Title</h2>
                <hr />
                <form method="POST" action="">
                    @Html.AntiForgeryToken()

                    @*@foreach (var receiving in Model.PurchaseItems)
                    {
                        <input type="hidden" id="PurchaseId" name="PurchaseId" value="@receiving.PurchaseId" />
                        <div class="custom-control custom-checkbox custom-control-inline">
                            <input name="selectedItems" id="@receiving.Product.Id" type="checkbox" class="custom-control-input" value="@receiving.Product.Id">
                            <label for="@receiving.Product.Id" class="custom-control-label zoom-3">@receiving.Product.Name / Quantity:@receiving.Quantity</label>
                        </div>
                    }*@
                    <!-- Button -->
                    <div class="form-group row">
                        <div class="col-sm-12">
                            <br />
                            <button type="submit" class="btn btn-primary btn-lg">Submit</button>
                            <a href="/Receive/CheckReceived" class="btn btn-secondary btn-lg">Cancel</a>
                        </div>
                    </div>
                </form>

            </div>
        </div>
    </div>
}